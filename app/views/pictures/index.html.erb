<h1>Total Pictures (<%= @pictures.length %>)</h1>
<%= link_to("New Picture", "/pictures/new") %>

<h1>Search pictures by year taken</h1>

<p>
  <%= form_tag("pictures/by_year", method: "post") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>
</p>

<ul>
  <% if @selected_year != nil %>
  <% @selected_year.each do |picture| %>
    <li><%= picture.title %></li>
    <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
  <% end %>
  <% end %>
</ul>

<h1>Photos from 2017</h1>
<% @photos_from_2017.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %></p>
  <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
<% end %>

<h1>Photos from 2016</h1>
<% @photos_from_2016.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %></p>
  <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
<% end %>

<h1>Photos from 2015</h1>
<% @photos_from_2015.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %></p>
  <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
<% end %>

<h1>Photos from 2014</h1>
<% @photos_from_2014.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %></p>
  <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
<% end %>

<h1>Most recent pictures</h1>
<ul>
  <% @most_recent_pictures.each do |picture| %>
    <li><%= picture.title %></li>
  <% end %>
</ul>

<h1>There are <%= @older_than_a_month.count %> pictures taken over a month ago</h1>
<ul>
  <% @older_than_a_month.each do |picture| %>
    <li><%= picture.title %></li>
  <% end %>
</ul>
