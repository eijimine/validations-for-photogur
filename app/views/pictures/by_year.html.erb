<ul>
  <% if @selected_year != nil %>
  <% @selected_year.each do |picture| %>
    <li><%= picture.title %></li>
     <%= link_to(image_tag(picture.url), "/pictures/#{picture.id}") %>
  <% end %>
  <% end %>
</ul>
